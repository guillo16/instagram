<%= simple_form_for @user do |f| %>
<% avatar_url =  current_user.photo || "https://www.w3schools.com/howto/img_avatar.png" %>
<%= cl_image_tag avatar_url, height: 200, width: 200, crop: :fill, class: "user-avatar" %>

<%= f.input :photo, input_html: { class: 'hidden' }, label_html: { class: 'upload-photo'}, label: '📷 Upload a photo' %>

<%= f.input :photo_cache, as: :hidden %>

<%= f.input :nickname %>

<%= f.button :submit, class: "btn btn-primary" %>
<% end %>
