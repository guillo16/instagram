<div class="review-form">
  <%= simple_form_for([ post, review ], remote: true) do |form| %>
  <div class="form-input">
    <%= form.input :content, as: :text, placeholder: 'Leave your coment ' %>
    <%= form.button :submit, "Submit Coment", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
